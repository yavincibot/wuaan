"use strict";
// import { Context } from "telegraf";
// const userInteractionCount: Map<number, number> = new Map();
// export const canInteractToday = (ctx: Context): boolean => {
//   const userId = ctx.from?.id||0;
//   const isAdmin = userId === 0; // admin's ID
//   if (!isAdmin) {
//     const count = userInteractionCount.get(userId) || 0;
//     userInteractionCount.set(userId, count + 1);
//     if (count >= 100) {
//       return false;
//     }
//   }
//   return true;
// };
